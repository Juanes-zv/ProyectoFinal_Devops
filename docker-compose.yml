version: '3.8'

services:
  todo-api:
    image: todo-api:latest
    container_name: todo_api_container
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=5000
    restart: unless-stopped
